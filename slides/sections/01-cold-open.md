---
layout: concept
heading: Find the ceiling
lines:
  - '"Add real-time notifications to Clash."'
---

<G01AgentLoop />

<!--
00:00–00:10. Fresh session, nothing pre-loaded beyond what CLASH ships. Type the naive
version of a real task, verbatim: "Add real-time notifications to Clash." Let it read
40 files. Let it guess at the notification model. Let it drift. Then open /context and
show the burn. Do not fix it — the thesis is demonstrated, not asserted: the ceiling isn't
the model, it's uncontrolled context.

Fallback, if it works instead of failing: the model is good and CLASH is well-structured,
so don't fake disappointment. Open /context anyway and make the point on cost instead of
failure — "it worked, and it burned 60% of the window to do it. Now imagine the fourth
feature in the same session." Same thesis, different evidence.
-->

---
layout: concept
heading: The bill comes due
lines:
  - "The ceiling isn't the model."
  - "It's uncontrolled context."
---

<G02ContextBudget />

<!--
This is the visual for whatever /context just showed on your real screen — walk the bands
bottom to top exactly as they filled: system prompt and CLAUDE.md are fixed and small; skills
loaded add a bit more; tool results and conversation are the two bands that grew without
limit while the agent drifted, and together they're what pushed the session toward the drift
zone at the top. Land the line hard: the ceiling every daily user of Claude Code eventually
hits is not a capability ceiling, it's a context-management ceiling. Everything for the rest
of the day is about controlling this bar on purpose instead of letting it fill by accident.
-->
